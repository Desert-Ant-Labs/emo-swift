# EmoAndroidExample

A tiny Android app for trying Emo with the Maven Central package
`ai.desertant:emo`.

## Run

Connect a device or start an emulator, then run:

```bash
./gradlew :app:installDebug
```

Type a short phrase and watch the suggested emoji update live. The first
suggestion downloads and caches the model.
